# Complete AI マスタープラン V2 — ミラー・リバーシ OFF 最強 AI

作成日: 2026-07-12
作成方法: 旧計画(complete_ai_plan.html / AI_PROGRESS_CHECKLIST.md)を前提にせず、コード・ルール・状態空間をゼロベースで再調査して策定
対象設定: `RulesConfig(enable_mirror=False, enable_reversi=False)` に固定(ミラー・リバーシ OFF)
運用: `AI_WORKFLOW_RULES.md` に完全準拠(各フェーズ末に確認成果物を提示し、ユーザー承認まで次工程に進まない)

---

## 1. 目的と「最強」の定義

目的はミラー・リバーシ OFF の Complete ルールで最強の AI を作ること。

このゲームは **完全情報・決定論・同時手番・二人ゼロ和のマルコフゲーム** である(`complete_solver/transition.py` で確認済み。乱数なし、隠れ情報なし、同時宣言のみ)。この構造のゲームには一意のゲーム値と定常マルコフ完全均衡(マキシミン戦略)が存在し、各状態の最適混合戦略は「子状態の値を並べた行列ゲームを LP で解く」ことで厳密に求まる。

したがって「最強」を次の3条件で定義する。

1. **非搾取性(近似ナッシュ)**: どんな相手戦略にも期待値で大きく負けない。混合戦略を含む均衡プレイ。
2. **搾取性**: 弱い相手(none/random 等の固定方策、旧モデル)には最短手数で確実に勝つ。
3. **人間妥当性**: 行動分布・初手・スキル使用が人間視点で説明可能(AI_WORKFLOW_RULES の目視ゲート)。

測定方法は §6 の採用ゲートで固定する。勝率単独では判定しない。

---

## 2. ゼロベース調査で確立した事実(2026-07-12 計測)

計画の根拠となる一次データ。すべて本日実測・実読した。

1. **ルールエンジンは高品質で再利用可能。**
   `complete_solver/`(state / actions / transition / matrix_game / finite_horizon / state_space)は純粋関数・immutable 設計で、132 テストが PASS。視点交代の符号処理(`same_turn_player`)も遷移層で正しく扱われている。**これはプロジェクトの中核資産であり全面的に残す。**

2. **全状態列挙(=全域厳密解)は不可能。**
   ミラー・リバーシ OFF で初期状態から BFS 列挙を実測した結果、8 分で到達 54.1 万状態・未展開フロンティア 43.3 万・拡張 1 回あたり新規状態 4〜7 個のまま減速せず。ストックの部分集合(2^8)× セメント × バフ類 × 両者分の組合せで、到達可能空間は少なくとも 10^7 オーダー以上と推定される。**「小さいゲームだから値反復で終わり」は成立しない。** 旧計画の `max_states=400〜500` の値反復は全体のごく一部を leaf=0 で打ち切った近似であり、そこから作った BC 教師(zero leaf)が数字偏重の根本原因だったことと整合する。

3. **深さ制限 LP 探索は既に存在するが遅い。**
   `FiniteHorizonSolver` は「LP バックアップ付き深さ制限探索」そのもの(=本計画の実戦エンジンの原型)。ただし素の Python で初期局面(TP 45 手 × NTP 9 手)の depth=3 が 15.7 秒、depth=4 が 121 秒。**高速化が第一級の課題。**

4. **GPU 環境あり。** Python 3.10 / torch 2.6.0+cu124 / CUDA 利用可。価値ネット学習は問題なし。

5. **旧路線(BC + MaskablePPO)の失敗は偶発ではなく構造的。**
   - zero-leaf VI 教師 → 数字 100% 偏重(境界アーティファクト)
   - 反応履歴 16 次元の観測 → 状態が非マルコフ化し、均衡概念と評価指標(exploitability)が壊れた
   - 決定論的初手(フェイント固定・スキップ固定)→ 同時手番ゲームでは決定的方策は原理的に搾取可能。ent_coef 調整では解決しなかった(item 42)
   - none 相手の無限ループ打ち切り → モデルフリー RL が「勝ち筋の発見」を報酬信号だけで行う限界
   これらは PPO の調整不足ではなく、**モデルフリー・方策ネット中心のアプローチがこのゲームの既知構造(完全情報・厳密遷移・LP で解ける局所均衡)を捨てていること**に起因する。

---

## 3. 新方針: 探索中心アーキテクチャ(LP バックアップ探索 + 価値ネット + 終盤厳密テーブル)

AlphaZero の構図を同時手番ゼロ和ゲーム用に置き換えたもの。方策ネットを主役にせず、**探索を主役**にする。

### 実戦時(プレイ)

```
着手 = 深さ制限探索(depth 2〜4, 適応)
        ├─ 各ノード: 子の値を並べた行列ゲームを LP で解き、値と混合戦略を得る
        ├─ 葉: 学習済み価値ネット V(s)(107 次元特徴量)
        ├─ 終盤・低複雑度状態: 厳密テーブルの値を直接参照
        └─ ルートの LP 混合戦略から実際に「サンプリング」して着手(決定論にしない)
```

- **混合戦略サンプリングが仕様の中心。** 同時手番ゲームの均衡は本質的に混合であり、これが旧モデルの「初手固定を突かれる」問題を原理レベルで消す。
- none のような受動的相手には探索が数手先の強制勝ちを直接発見する。γ<1 により最短勝利が選好されるため、無限ループ打ち切りは起こり得ない。

### 学習時(fitted Nash value iteration)

```
繰り返し:
  1. 自己対戦で状態を収集(双方が現行探索方策、LP 混合からサンプル + ε ランダム注入)
  2. 収集した各状態に「depth-d LP バックアップ値(葉=現行ネット)」を教師値として付与
  3. 終端値・終盤厳密テーブル値をアンカーとして混ぜ、V(s) を回帰学習
  4. 新旧ネットで世代対戦・テーブル一致度を測り、改善していれば採用
```

これはマルコフゲームに対する fitted Nash-VI(minimax-Q の関数近似版)であり、完全情報ゼロ和では値が一意なので、方策の非定常性・忘却(PPO で起きた catastrophic forgetting)の問題構造がそもそも生じない。**学習対象は「値」だけ**で、方策は常に探索+LP が現場で導出する。

### 旧路線の問題がどう消えるか(対応表)

| 旧問題 | 本アーキテクチャでの帰結 |
|---|---|
| zero-leaf 教師の数字偏重 | 葉は学習済み V + 終端/テーブルアンカー。leaf=0 境界が存在しない |
| 初手決定論(フェイント/スキップ固定) | LP 混合戦略からサンプリング。均衡混合が仕様として保証 |
| none 相手の無限ループ | 探索が強制勝ちを直接読む。γ<1 で最短勝利選好 |
| 反応履歴で非マルコフ化 | 状態は完全観測でマルコフ。履歴特徴量は不要(廃止) |
| exploitability 指標の破綻 | BR 攻撃エージェントによる実測搾取率に置換(§6) |
| PPO の視点符号バグ類 | solver の遷移層(検証済みコード)だけを使う |

---

## 4. 既存資産の取捨

### 残す(そのまま/軽微修正)
- `complete_solver/` 全部 + テスト 132 件(中核エンジン)
- `complete_rl/obs.py` の先頭 107 次元(= solver `State` の Markov エンコード)→ 価値ネット特徴量。**反応履歴 16 次元は廃止**
- `complete_rl/env.py` + named NTP policies(none/counter/block/random/分離系)→ **評価ハーネス**および BR 攻撃者の訓練環境として
- `separated_policy_report.py` / `start_side_policy_diagnostics.py` → 行動目視ゲートに再利用
- `AI_WORKFLOW_RULES.md`(運用ルール)

### 主軸から外す(アーカイブ。削除はしない)
- BC 事前学習路線(`bc_pretrain.py`、depth 教師、leaf mode 一式)
- MaskablePPO / PerspectiveMaskablePPO を「本体」とする路線 — ただし **§6 の BR 攻撃者として再利用する**
- episode NTP 切り替え学習方式、反応履歴観測、curriculum / fine-tune 路線
- `exploitability.py` の空履歴 BR 指標(限界確認済み)
- 旧学習済みモデル群 — **評価用対戦相手としてのみ残す**

### 凍結
- `AI_PROGRESS_CHECKLIST.md` は歴史記録として凍結し、進捗管理は本ファイル §8 のチェックリストに一本化する。

---

## 5. フェーズ計画

各フェーズの末尾に確認成果物(HTML/表)+ AI の解釈を提示し、**ユーザー承認後に次へ進む**。

### N1. 高速探索基盤(最優先・計画全体の律速)
- 内容:
  - 遷移・合法手生成の結果キャッシュ、状態の安定ハッシュ、**探索間で持続するトランスポジションテーブル**
  - **純粋鞍点チェック**(行列に純粋均衡があれば LP を呼ばない — 大半のノードで LP 回避見込み)
  - **ノード内ダブルオラクル**(45×9 の全子展開をやめ、ベストレスポンスを交互に追加して小さい部分行列で均衡を得る)
  - 明白劣位行動の枝刈り(例: 合計 0〜4 の数字宣言のうち文脈上無意味なもの)
- 完了条件: 既存 132 テスト PASS のまま、初期局面 depth=3 ≤ 1 秒・depth=4 ≤ 10 秒(現状 15.7s / 121s)。
- 確認成果物: 高速化前後の値・方策の一致検証表、速度ベンチ HTML。
- 判断点: 目標未達なら Numba / Rust コア化をここで見積もる(先送りしない)。

### N2. 終盤・低複雑度厳密テーブル
- 内容: 「ストック空 or 小」「hands 合計が少ない」等の部分空間を実測列挙し、規模が許す範囲(目安 10^5〜10^6 状態)を厳密値反復で解いてディスク保存。探索から O(1) プローブ。
- 完了条件: テーブル規模・値分布・代表局面の妥当性レポート。探索接続テスト PASS。
- 意義: (a) 実戦で終盤は文字通り最適、(b) 価値ネットの正解アンカー、(c) ネット精度の恒久的な検証基準。

### N3. 価値ネット v0
- 内容: 107 次元 → MLP(目安 2〜3 層 × 512)。初期教師データ: ランダム/浅い探索プレイアウトで収集した状態への depth-2〜3 バックアップ値 + N2 テーブル値。
- 完了条件: N2 テーブル上での MSE・順位相関レポートで `material_leaf_evaluator` を明確に上回る。
- 確認成果物: 代表局面の V(s) と厳密値の比較表。

### N4. fitted Nash-VI 自己対戦ループ(本体)
- 内容: §3 の学習ループを世代制で回す。世代ごとに (a) 新旧ネット世代対戦、(b) テーブル一致度、(c) 教師値の分散・発散チェック。ε ランダム状態注入で分布カバレッジを確保。
- 完了条件: 3 世代以上連続で世代対戦・テーブル一致度が悪化しないこと。行動分布 HTML の人間確認。
- リスク対応: 発散したらターゲットネット固定・学習率調整・アンカー比率増で戻す(その時点で停止して報告)。

### N5. 実戦エージェント確定
- 内容: 時間予算に応じた適応深度、テーブルプローブ、混合サンプリング(検証用に決定論モードも用意)、人間対戦用 CLI。
- 完了条件: 対戦 CLI で人間がプレイ可能。挙動レポート承認。

### N6. 評価ゲート(§6 を実施)
- 内容: named NTP 方策一式・旧ベスト PPO 2 モデルとの対戦、**BR 攻撃**(既存 MaskablePPO 基盤を攻撃者として凍結エージェントに 250k steps × 複数 seed 訓練)、行動レポート。
- 完了条件: §6 の全ゲート通過 + ユーザー最終承認。

### N7. 反復強化(継続) — 「深さを掘る以外」で強くする

戦略考察と設計案の全文は `N7_STRATEGY_AND_DESIGN.md`。要旨:

- **族(LP バックアップ探索 + 価値ネット + fitted Nash-VI)は変えない。** これは AlphaZero を同時手番
  ゼロ和に正しく一般化した先。MuZero/CFR/PSRO への乗り換えは真のボトルネック(長期スキルの信用割当)を
  解かず、完全情報+厳密シミュレータの強みを捨てるので不採用。
- **深さ4は主戦略にしない。** 遅効性スキル(セメント/ストック/ロック)は約10プライ先で報われ、深さ4
  (葉+2プライ)では構造的に届かない。真のレバーは **価値ネットの長期精度と訓練カバレッジ**
  (ネットが完璧なら深さ1で足りる=深さ探索は穴埋めにすぎない)。

採用する強化路線(優先度順):

- **A(本命)— 到達可能サブグラフ Nash 値反復**: 決定論+完全情報を使い切り、深さを掘らずに長期価値を
  サブグラフ全域へ伝播させて教師値を作る。A0 厳密DBの機構(`packed_vi` の `_enumerate`/`_jacobi_sweep`/
  `PackedEndgameDB`、A0=33.4万状態を54スイープ25秒で収束の実績)を「閉じた宇宙→サンプリング領域+
  フロンティアをネットで閉じる」形に一般化。教師差し替えで B/C/D すべての土台になる。
- **B — 価値ネットの構成エンコーダ化**: フラット特徴で潰れる非線形スキルコンボを、保持スキル集合/盤面への
  集合・注意エンコーダで表現。A の後(厚い教師の後)が相性最良。
- **C — 選択的/最良優先探索(smarter, not deeper)**: 均衡台(support)上の重要ラインだけ深掘りする反復
  深化で、深さ4同コストで実効深さ6〜8。A と並行可。
- **D — 深さ4教師**: インフラ完成済みで安価。A の効果測定の対照(=深さでは届かないことの実証)に随時活用。

(旧 N7 メモ: depth 引き上げ、データ増、ネット拡大、コアの Rust 化、γ 感度 0.997〜0.999 最終確定、
弱点局面の的当て学習 — これらは上記に統合/継続。)

想定順序: A(先行)→ C は A と並行可 → B は A の後 → D は随時。各フェーズ末に確認成果物+解釈を提示し
ユーザー承認後に次へ。

想定順序は N1 → N2 → N3 → N4 → N5 → N6(→ N7 反復)。N2 と N3 は一部並行可。

---

## 6. 採用ゲート(最終合格条件)

1. **固定方策制圧**: none / counter_first / block_first / random / 分離系(none_uniform 等)すべてに勝率 ≥ 99%、打ち切り 0(none 含む)。
2. **旧世代超え**: 旧ベスト PPO(`maskable_ppo_bc_standard_episode_mixed`、`maskable_ppo_bc_depth4_standard_proper`)との直接対戦で勝率 ≥ 70%。
3. **実測非搾取性**: 凍結した本エージェントに対し BR-PPO 攻撃者(250k steps × 3 seeds)を訓練しても、本エージェント側勝率 ≥ 55% を維持。
4. **人間妥当性**: 初手分布・スキル使用率・開始側手順レポートで警告 0 + ユーザー目視承認。
5. 上記すべてを **開始側/後手側の両視点**で集計する(旧レポートの視点混同バグの教訓)。

---

## 7. リスクと対策

| リスク | 対策 |
|---|---|
| N1 の速度目標未達 | ダブルオラクル・キャッシュ・並列自己対戦 → それでも不足なら Numba/Rust コア化を N1 末で即断(計画の律速なので先送りしない) |
| 価値ネットがストック組合せに汎化しない | ストックはスキル別 bit で特徴量化済み。N2 テーブルをアンカー・検証基準に常用。ランダム局面注入で分布拡張 |
| ブートストラップ学習の発散 | 終端値+テーブルの固定アンカー、ターゲットネット、世代ゲートで退行検出したら停止・報告 |
| LP 縮退・タイ | 既存 tolerance 処理を維持。値タイは「短手数勝ち優先」でタイブレーク |
| γ の恣意性 | 旧 gamma sweep で感度小を確認済み。N7 で 0.997/0.999 比較を最終確認 |
| 「探索が読み切る前提」の穴(超長期戦術) | depth 不足分は V が吸収する設計。N6 の BR 攻撃が最終的な安全網 |

---

## 8. チェックリスト

- [x] N0: 本計画のユーザー承認(2026-07-12。付帯条件: ①作業ごとに WORK_LOG.md へ記録 ②専門解説+平易解説の二重解説 ③タスクごとの所要時間見積もり提示)
- [x] N1: 高速探索基盤 — 実装完了(2026-07-12)。値一致検証済み・ベンチ HTML 生成済み。コールド速度は d3=1.7s / d4=12.7s で目標(1s/10s)まで約2倍残り(warm 再解決は 0.3ms)。残り2倍にはコンパイル化が必要 → 案A(N2/N3先行、実測後判断)/案B(即コンパイル化)をユーザーに提示中
- [x] N1 承認(2026-07-12 案Aで承認: コンパイル化は N4 前に実測スループットで判断。コールド d3=1.7s/d4=12.7s を暫定合格とし N2 へ)
- [x] N2: 終盤厳密DB — 機構完成(2026-07-12)。閉集合列挙+厳密VI+永続化+探索プローブ、参照実装との一致テスト済み(全171テストPASS)。計測の結果、(1,1)終盤の閉集合はすべて巨大(空ストックで140万超)で、オンデマンド方式ではなく「(1,1)宇宙の一括バッチ解決」が本命と判明 → コンパイル化が前提
- [x] N2 承認(2026-07-12 案C改で承認: ストック重要組限定の縮小(1,1)テーブル → 除外スキルの自動重要度検証(逸脱利得スキャン)→ 必要ならアルファベット拡張。初期重要組: フェイント・ロック・フラッシュ・ガード・スキップ。クイックは検証で自動判定。コンパイル化はテーブルの前提から外れ N4 前に実施)
- [x] N2b: 終了(2026-07-12)。成果: コンパイル核(packed_engine/packed_vi、差分テスト検証済み、列挙 Python比約1000倍)+ A0(ストック禁止)宇宙38.3万状態の厳密解。結論: **網羅的終盤テーブルは構造的に不可能**(A4でも3000万超、フルは推定250億)。「保持1個」抽象化はユーザー指摘(ストックは2個以上で真価を発揮)により破棄。終盤の正確さは探索の終端読み切り+価値ネットで担保する設計に一本化し、N3 へ
- [x] N2b 承認(ユーザーの方針確認「ディープラーニングが必要では」→ その通りで N3/N4 が当初から主軸、と整理して合意形成)
- [x] N3: 価値ネット v0 — 完了(2026-07-12)。material_leaf 超えを3ものさし全てで達成(検証MSE 137倍差、深さ3順位相関 0.947 vs 0.598、A0厳密スライス 0.919 vs 0.338)。ドメイン知識を特徴量に反映。成果物 results/n3_value_net_v0_report.html
- [x] N3 承認(2026-07-12「N4進めてください」で確認)
- [x] N4(v1): fitted Nash-VI 世代ループ — 12世代完走(2026-07-12)。機構健全(クラッシュ0・警報0・打切り0.3%以下)。**ネット寄与は明確(vs material 66%)だが世代の積み上げは横ばい(vs v0 51.7%)**。原因3点特定(分布狭窄・教師浅い・錨なし)→ N4b で対処。成果物 results/n4_generation_loop_report.html
- [x] N4 確認・承認(2026-07-12 案Eで承認: N5・評価基盤を先行、N4b はものさし確立後)
- [x] N4b: ルール3訂正(フェイント参照/ガード1フェーズ1回/真スキップ)を経て、多様化+錨リプレイ+**深さ3教師**で再構築(2026-07-13)。世代の積み上げが機能(vs v0 55.3%、A0相関単調改善→0.827)。長期スキルはロック/ストックが終盤から芽吹き(「序盤セメント>終盤」のリトマスは未達 — 深さ増強が将来課題)
- [x] N6(ゲート④): BR攻撃テスト(2026-07-13)— 攻撃者3seed×250k、勝率47.3〜48.0%(プール47.7%±3.3)。**非搾取性PASS**(基準を理論値に較正: ナッシュ完全体でも攻撃者はゲーム値≈50%に届くため「50%を超えられない」が正しい合格線)
- [x] N5a: 実戦エージェント+ベースライン評価+対戦CLI(2026-07-13)。LP混合が「none無限ループ」病理を完治させる実証を獲得(決定論177/300打切 vs 混合0)。ベースライン勝率75〜89%、material対照に全面優位。**発見: 旧「99%」ゲートは旧評価仕様の産物で公正な両席分離評価では不成立 → §6ゲートの再定義を提案中**
- [x] N5b: 旧PPOベストとの直接対戦(2026-07-13)— 旧best1に63〜75%、旧best2に54.5〜56.5%で**両方に有意な勝ち越し**(新ゲート③クリア。旧best2への差は薄く N4b の動機)
- [x] N5 承認(2026-07-13 ゲート再定義込みで承認済み)
- [ ] N6: 評価ゲート全通過(§6)
- [ ] N6 最終承認 = ミラー・リバーシ OFF 最強 AI v1 完成
- [ ] N7: 反復強化(継続) — 戦略再検討完了(2026-07-14、`N7_STRATEGY_AND_DESIGN.md`)。族は維持、深さは主戦略にしない方針を確立。採用路線 A(サブグラフNash-VI・本命)/B(構成エンコーダ)/C(選択的探索)/D(深さ4・対照)。設計案 A/B/C 執筆済み、実装は A から着手予定
  - [~] N7-A: 到達可能サブグラフ Nash 値反復 — A1〜A6 完了・**実現可能性実証(2026-07-14)**。
    - A1〜A4: `complete_solver/subgraph_vi.py`(開サブグラフ Nash-VI、`packed_vi` の sweep/repair を無改造再利用)+ 参照テスト3件。A0 厳密宇宙で閉集合=solve_universe一致・開集合+厳密境界=真値回復を実証(全3件PASS)。
    - A5〜A6: `complete_ai/graph_teacher.py`(ネット結線)+ `complete_ai/n7a_feasibility.py`(実測)。**グラフVI教師は生ネットより対A0厳密値で一様に良い**(mean_abs ほぼ半減 0.14→0.06、spearman +0.07〜0.09)=案Aの狙い(深さを掘らず長期信号注入)を小規模実証。ストール検知追加。
    - ①ダンピング(ω=0.5)で plateau 解消済み(converged 9.9e-7、精度不変=不動点保存を実証)。②世代ループ結線済み(`generation_loop --teacher graph-vi`)。③実ゲーム端到端スモークOK(ω=0.5 clean収束)。
    - 本番12世代 graph-vi 完走(別名空間 tag=gvi、深さ12世代ベースライン保存)。**a0相関で全世代 深さ版を上回る**(final 0.8189 vs 0.8143、教師改善を実証)。ただし**リトマス(序盤セメント>終盤)は両モデルとも未達**(序盤0% vs 終盤~4.5%)。長期スキル使用は微増(セメント0.30→0.38%)。
    - 新ボトルネック=**カバレッジ密度**(frontier率89%・種がオンポリシーのみ)。generation_loop に `--tag` 名前空間 + `--graph-coverage`(ランダムウォーク種)追加済み。
    - **カバレッジ強化実験(gvicov 12世代)完了 — 正直な結論: リトマス依然未達**。3モデル序盤セメント全て0%(depth/gvi/gvicov)、セメント総使用は微増(0.31→0.45%)のみ。**構造的限界を特定**: frontier88%の境界ネットの盲点(セメントの~10プライ先payoffを知らない)が内部へ伝播し、graph-VIは「ネットに無い長期信号」を作れない。→ A は教師品質改善としては確立だが長期スキル開花は A+カバレッジ単体では不可。真の突破は世代横断の大量種蓄積/別機構が要る(貴重な負の結果)
  - [ ] N7-B: 価値ネット構成エンコーダ化(設計済み、A の後)
  - [~] N7-C: 選択的/最良優先探索 — 実装+実証+実戦搭載(2026-07-14)。`complete_ai/selective_search.py`(support上のみ深掘りする反復深化、tau<0=uniform一致で検証、3件PASS)。**選択的で uniform depth-3 を約130分の1のセル数(値差0.019)、depth-5 が uniform depth-3 の約1/13コスト**。**`SelectiveSearcher.solve()` を BatchedSearcher と同 signature で実装→実戦エージェントに drop-in、`play_cli --selective-depth`**。検証: 同一ネットで selective vs 深さ2 = 深さ4 0.525/深さ5 0.558(単調に強い、追加学習ゼロ)。残: **njit コンパイル版**(exploitability測定/深さ5教師を解禁する本命ピース)
  - **方針確定(2026-07-14)**: リトマス(長期スキル開花)は非目標化。最優先=値の正確さ/exploitability(神視点最適解への近さ)。未使用スキルは最適解で真に不使用の可能性ありとユーザー明言
  - **~~【決定的】exploitability 比較(2026-07-14)~~【撤回: バグの産物と判明】**: 旧測定 graph-vi 0.440 vs depth 0.540 は**バグ版モデルでの測定**。バグが depth 側を不当に悪化させていた。→ 下記の修正済み再測定で撤回。
  - **【更新】exploitability 修正済み再測定(2026-07-14)**: 修正済み両モデルに BR攻撃者(PPO 150k×2seed)を再学習。**graph-vi worst 0.487(PASS) vs depth worst 0.480(PASS)** =**両者ほぼ同値(差0.007=ノイズ内)、どちらも非搾取**。バグ修正で depth が 0.540 FAIL→0.480 PASS へ大改善。a0 も僅差(gvi 0.8375 vs depth 0.8352)。→ **「graph-vi 明確優位=本採用確定」の根拠は消失。両者ほぼ同等が正直な現状**。決着は graph-BR(厳密exploitability)finalize 待ち。data/n6_br_attack_{gvi,depth}.json。**本採用モデルの選択はユーザー判断待ち**
  - **スキップ局面バグ修正の再生成【範囲完遂】(2026-07-14)**: `_matrix_value` 証明失敗ノードで mid プレースホルダを採用していた `expand_depth3`/`root_value_only` バグの修正後、**本採用 graph-vi 本番モデルだけがバグ教師のまま(a0 0.8189)放置**されていたのを発見。v0アンカー→graph-vi 12世代→depth 12世代を修正済みコードで統一再生成。**graph-vi a0 0.8189→0.8375(+0.019)**、depth 0.8352。回帰テスト `test_extra_turn_state_matches_exact_solver` PASSで修正有効化を確認。**A0厳密DB/graph-vi内部は影響なし**と確定(`packed_vi._jacobi_sweep`+`_repair_state_value` が失敗セルをscipy厳密修復して公開=前回宿題「VI側修復の妥当性」を解決)。退避 archive_pre_rerun_2026-07-14/。litmusは未逆転(非目標)。**要更新: 下記 exploitability 比較(0.440 vs 0.540)は旧バグ版での測定 → 修正済みモデルで再測定待ち**
  - **レジーム切替の定量確認(2026-07-14、Part 6-3c)**: タイム発動中はネット絶対誤差+25〜32%悪化(両ネット一貫)。相手はタイムを使えるので非搾取性にレジーム対応必須。挟み撃ちテーブルはタイム/ストック次元込みで。計器 `n7_regime_slices.py`
  - **graph-BR(厳密exploitability)機構構築(2026-07-14、`graph_br.py`)**: 連立VI(V_mover/V_reactor)で攻撃者の厳密BR。小規模で収束・式健全。**但し到達集合(攻撃者任意×凍結support)は40万でも閉じず(>400k、推定10^6)、純Python列挙555状態/秒=実用外**。finalize要: 凍結方策バッチ化+VI njit化 or 深さ制限BR+厳密終盤テーブル葉(挟み撃ち)。当面 exploitability 主指標は PPO 版。予備数値0.65は打ち切り過大で信用不可
  - [~] **N7-E: graph-BR finalize 着手(2026-07-14) — 挟み撃ちで前進、純Python厳密は壁**。`graph_br` に終盤テーブルキャップ(A0局面を厳密リーフ化・338,841件)+凍結=PincerSearcher(配信版)。**厳密(無制限深さ)は到達集合がcap40万でも閉じず=純Python不可(ストック/中盤爆発、推定10^6を実証)**。深さ制限BR(`max_depth`, D先は凍結ネット値キャップ)実装。**結果: D=4 exploitability 0.5169 / D=5 0.5283(クリーン) / D≥6 は集合>25万+VI約55分で非現実的**。**所見: exploitabilityは深さで単調上昇・頭打ちにならない=深い(ストック等長期)搾取の存在示唆**。PPO0.44<D=4 0.517<D=5 0.528でPPOが弱い下界だったことを実証。**本命finalizeは njitコンパイル(solveバッチ+VIベクトル化)で深いBRを可能にすること(大規模)。ストック/序盤の搾取は「浅くは小・深さで増大・深部未計測」が正直な現状**。ツール n7_graph_br(`--max-depth`)、log data/graphbr_*。
  - [~] **N7-E続: graph-BR njitフル改修 着手(2026-07-14)**。既存`graph_br.py`(純Python)温存、新規`graph_br_fast.py`にnjit VI追加、`n7_graph_br --engine python|njit`(既定njit)で切替。列挙は共有。njit VI設計=葉定数折り込み(反復非依存セルをper-row定数化→内部エッジのみ走査)+nogil連立Jacobi。parityテスト(njit==python, トイ+fuzz)PASS。**効果: 律速だったVI(250k≈55分)→njit実質数秒、D=6解禁**。残律速=列挙(torch solve 500/s)→D≥7には列挙バッチ化(次の本丸)。
  - [ ] **N7-E(旧記述・参考): 未使用スキルによる搾取可能性の厳密判定**。ユーザー懸念(2026-07-14): 「ストック/ロック/セメント/チャージ/タイム等、今ほぼ0%のスキルが神視点では実は強く、それを駆使する方策が現行モデルを搾取できるのでは?」。**現時点で否定不能=実在リスク**(理由: 唯一の厳密計算A0はストックレス終盤のみ/PPO-BRは長期スキルに弱い下界で盲点あり/2モデルは分布ほぼ同一=同じ盲点を共有→第三の方策が両者を破りうる)。決着には graph-BR をストック・タイム次元込みで finalize する必要(凍結方策バッチ化+連立VI njit化 or 深さ制限BR+厳密終盤テーブル葉)。**重い作業につき保留**。安価な部分検証として A0厳密(ストックレス終盤)の神視点policy vs モデルを先行(下記 N7-F)
  - [x] **N7-F: A0厳密の神視点 policy vs モデル policy 比較(2026-07-14 完了)**。神=mask=0の1手LP+`solve_small_zero_sum`混合、**LP値=A0値(max|Δ|2.8e-10)で正しさ保証**、8000局面。**結果(ストックレス終盤): 神も ロック/セメント/チャージ/タイム/ガード/ストック=すべて0.0% → これらの不使用は盲点でなく終盤では最適=懸念は終盤についてはシロ**。唯一の差: モデルはフェイント使いすぎ(神0% vs 6.6〜7.0%)+数字宣言少なめ(神47.3% vs 39.8%)=軽微。A0 DB は現行ルールで再ビルド一致(209,698状態・値max|Δ|1.2e-6)=currency確定、data/endgame_h11_A0_current.npz。**限界: ストック自体と序盤の価値は測定不可 → graph-BR(N7-E)待ち**
  - [x] **N7-F(a): 終盤マージ=A0厳密テーブルを実戦エージェントに接続(2026-07-14 完了)**。`complete_ai/endgame_table.py`(`EndgameTablebase`: A0値を葉にした1手LPで厳密最適mixtureを返す、BatchedSearcher.solveと同signature、状態キャッシュ付)+ `SearchAgent(endgame=)` drop-in + `play_cli`(既定ON、`--no-endgame-table`)。**適用範囲=両者手1本かつストック無**(それ以外はネットへフォールバック)。文脈(ガード/タイム/必殺/参照スキル)は状態キーに内包され自動反映。単体テスト4件PASS(値=A0一致1e-6・分布妥当・境界・エージェント経由でネット不呼出)。**実測: (1,1)手の89.5%で発火、発火局面のフェイント率0.00%=神と完全一致(=「フェイント使いすぎ」ズレを是正)**。残10.5%はh11閉包外の(1,1)。
  - [x] **N7-F(b): 挟み撃ち(pincer)=探索の葉をA0厳密値に差替(2026-07-14 完了)**。`endgame_table.PincerSearcher`(BatchedSearcherの`_net_values`だけをoverride: 葉キーがA0にあれば厳密値、無ければネット値。**訓練/nogil経路は非改変=対局専用**)。`play_cli` は endgame有効時にpincerを既定採用(selective時を除く)。単体テスト追加(A0局面でpincer値=厳密値, max|Δ|<0.02)。**実測(1500 A0局面): 深さ2バックアップ誤差 net-only mean0.037/max0.24 → pincer mean0.0000/max0.006 = ネット終盤誤差をほぼ100%除去、net vs pincerで値>0.01差が51%**。→ 終盤"手前"の判断もネット誤差なしに。次: play_server配線(任意・EXE要再ビルド)、A0閉包を実(1,1)入口へ拡張で発火率↑
  - [ ] N7-D: 深さ4教師(インフラ完成済み、随時)

## 9. 記録

- 2026-07-12: ゼロベース再調査(状態空間実測 54.1 万+/8 分・フロンティア拡大継続で全列挙不能と判定、depth3=15.7s/depth4=121s 実測、torch+CUDA 確認)に基づき本計画を作成。旧チェックリストは凍結。
