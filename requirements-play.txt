# 対戦するだけに必要な最小の依存（AIと対戦.bat / python -m complete_ai.play_server）
# インストール:  pip install -r requirements-play.txt
#   ※ torch は環境に合わせて。GPU不要（CPUでOK）。軽くしたいなら CPU版:
#      pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
numba>=0.58
numpy>=1.24.0
scipy>=1.10.0
